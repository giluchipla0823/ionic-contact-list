import { AnimatedFabDirective } from './animated-fab.directive';

describe('AnimatedFabDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedFabDirective();
    expect(directive).toBeTruthy();
  });
});

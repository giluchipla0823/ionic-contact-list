import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-contact-list',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
